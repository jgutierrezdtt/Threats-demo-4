services:
  web:
    build:
      context: ./apps/web
    ports:
      - "8080:80"
    depends_on:
      - api

  api:
    build:
      context: ./apps/api
    environment:
      PORT: "3001"
      CORS_ORIGIN: "http://localhost:8080"
    ports:
      - "3001:3001"
